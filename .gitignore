# --- Raw data & scratch (NEVER commit; only aggregates live in data/) ---
.scratch/
*.tar
*.tar.a*
*.tar.gz
traces/
heatmap/
*_result.json
sample_trace.bin

# Any large file must never enter history (Pages: 100MB/file, ~1GB repo).
# Aggregates in data/ are small (<2MB/day) and ARE committed.

# --- The overnight handoff brief (private, not part of the deliverable) ---
cc_handoff_a1_gps_interference_archive.md

# --- Working notes: kept LOCAL as working tools, never published (publish
#     decision, 2026-07-15). These live on disk for session continuity, design
#     truth, and the launch runbook; they simply do not ship in the public repo. ---
board/
docs/reviews/
docs/self_audit_report.md
DESIGN.md
LAUNCH_DAY.md
docs/faq_draft.md
docs/writeup_outline.md
docs/writeup_draft.md

# --- This publish-decision review aid (local only) ---
PUBLISH_REVIEW_BUNDLE.md

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.mypy_cache/

# --- Node / frontend tooling (site is buildless; no node_modules committed) ---
node_modules/

# --- OS / editor ---
.DS_Store
Thumbs.db
.vscode/
.idea/

# assembled local preview (deploy builds this on the runner)
_site/
